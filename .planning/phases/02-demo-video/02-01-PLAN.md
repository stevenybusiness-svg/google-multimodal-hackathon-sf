---
phase: 02-demo-video
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-demo-video/preflight-report.txt
autonomous: true
requirements:
  - SUBMIT-02
must_haves:
  truths:
    - "No secrets (.env values, API keys, tokens) are committed to the git repository"
    - "The .gitignore file blocks .env and credentials files"
    - "The Cloud Run URL returns a healthy response"
    - "Slack bot token is functional and can post to the configured channel"
  artifacts:
    - path: ".planning/phases/02-demo-video/preflight-report.txt"
      provides: "Pre-flight verification results for all demo dependencies"
      contains: "PREFLIGHT"
  key_links:
    - from: ".gitignore"
      to: ".env"
      via: "gitignore pattern"
      pattern: "\\.env"
---

<objective>
Verify the demo environment is fully operational before recording. Confirm no secrets are committed to git (SUBMIT-02), the live Cloud Run URL is healthy, and all 4 action backends (Calendar, Slack, task log, doc revision) are reachable.

Purpose: Prevent a failed demo recording by catching issues before the human starts Loom.
Output: preflight-report.txt confirming all checks pass or listing blockers.
</objective>

<execution_context>
@/Users/stevenyang/Documents/google-devpost-hackathon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stevenyang/Documents/google-devpost-hackathon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-demo-video/02-CONTEXT.md
@.planning/phases/01-deploy-auth/cloud-run-url.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify no secrets in git and .gitignore guards</name>
  <read_first>.gitignore, .env</read_first>
  <files>.planning/phases/02-demo-video/preflight-report.txt</files>
  <action>
Run a series of checks to confirm SUBMIT-02 (public GitHub repo has no secrets):

1. Verify `.gitignore` contains entries for `.env`, `credentials.json`, and `*.json` token files.
2. Run `git log --all -p -- .env` to confirm `.env` was never committed to git history.
3. Run `git log --all -p -- credentials.json` to confirm credentials.json was never committed.
4. Search the entire tracked tree for common secret patterns: `grep -rn "GOOGLE_API_KEY=AIza\|SLACK_BOT_TOKEN=xoxb\|GOOGLE_CALENDAR_TOKEN_JSON=" --include="*.py" --include="*.js" --include="*.html" --include="*.md" .` (should find zero matches with actual key values; env var NAMES in docs are fine).
5. Verify the repo has no large binary blobs that might contain secrets: `git ls-files | xargs file | grep -i "data\|binary"`.

Write results to `.planning/phases/02-demo-video/preflight-report.txt` starting with:
```
PREFLIGHT CHECK: Git Secrets Audit
===================================
.gitignore guards .env: [PASS/FAIL]
.env never committed: [PASS/FAIL]
credentials.json never committed: [PASS/FAIL]
No hardcoded secrets in source: [PASS/FAIL]
No suspicious binaries: [PASS/FAIL]
```

If any check FAILS, stop and report the issue. Do NOT proceed to Task 2.
  </action>
  <verify>
    <automated>grep -c "PASS" .planning/phases/02-demo-video/preflight-report.txt | grep -q "5" && echo "ALL 5 CHECKS PASSED" || echo "SOME CHECKS FAILED"</automated>
  </verify>
  <acceptance_criteria>
- preflight-report.txt exists and contains exactly 5 lines with "PASS"
- No line contains "FAIL"
- .gitignore contains ".env" pattern (grep -q "\.env" .gitignore returns 0)
  </acceptance_criteria>
  <done>All 5 git-secrets checks pass. Repository is safe to make public.</done>
</task>

<task type="auto">
  <name>Task 2: Verify live Cloud Run URL and demo backends</name>
  <read_first>.planning/phases/01-deploy-auth/cloud-run-url.txt, backend/main.py</read_first>
  <files>.planning/phases/02-demo-video/preflight-report.txt</files>
  <action>
Append to the existing preflight-report.txt with live service checks:

1. `curl -s -o /dev/null -w "%{http_code}" https://meeting-agent-31043195041.us-central1.run.app/health` — expect 200.
2. `curl -s https://meeting-agent-31043195041.us-central1.run.app/health` — expect `{"status":"ok"}`.
3. `curl -s -o /dev/null -w "%{http_code}" https://meeting-agent-31043195041.us-central1.run.app/` — expect 200 (serves the UI page).
4. Verify the UI page contains expected elements: `curl -s https://meeting-agent-31043195041.us-central1.run.app/ | grep -c "Detected Actions"` — expect >= 1.
5. Verify Slack bot token: `curl -s -H "Authorization: Bearer $SLACK_BOT_TOKEN" https://slack.com/api/auth.test | grep -q '"ok":true'` — expect ok:true. Load SLACK_BOT_TOKEN from .env first.

Append to preflight-report.txt:
```

PREFLIGHT CHECK: Live Service
===============================
/health returns 200: [PASS/FAIL]
/health returns {"status":"ok"}: [PASS/FAIL]
/ serves UI page: [PASS/FAIL]
UI contains "Detected Actions" panel: [PASS/FAIL]
Slack bot token valid (auth.test ok:true): [PASS/FAIL]

PREFLIGHT SUMMARY
=================
Total checks: 10
Passed: [N]/10
Status: [READY FOR DEMO / BLOCKERS FOUND]
```

If any service check fails, note it but continue checking the rest so the user gets a full picture.
  </action>
  <verify>
    <automated>grep "PREFLIGHT SUMMARY" .planning/phases/02-demo-video/preflight-report.txt && grep "Status:" .planning/phases/02-demo-video/preflight-report.txt</automated>
  </verify>
  <acceptance_criteria>
- preflight-report.txt contains "PREFLIGHT SUMMARY" section
- preflight-report.txt contains "Status: READY FOR DEMO" (all 10 checks pass)
- curl to /health returns HTTP 200 (verifiable by re-running curl)
- Slack auth.test returns ok:true
</acceptance_criteria>
  <done>All 9 preflight checks pass. Cloud Run URL is live and serving the UI correctly. Environment is ready for demo recording.</done>
</task>

</tasks>

<verification>
- preflight-report.txt exists with 10 PASS results
- No FAIL entries in preflight-report.txt
- Status line reads "READY FOR DEMO"
</verification>

<success_criteria>
- SUBMIT-02 verified: no secrets in git history, .gitignore guards in place
- Live Cloud Run URL confirmed healthy and serving expected UI
- All results documented in preflight-report.txt for reference during recording
</success_criteria>

<output>
After completion, create `.planning/phases/02-demo-video/02-01-SUMMARY.md`
</output>
