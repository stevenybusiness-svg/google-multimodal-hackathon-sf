---
phase: 01-deploy-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.env]
autonomous: false
requirements: [DEPLOY-02]
user_setup:
  - service: google-calendar-oauth2
    why: "OAuth2 requires browser sign-in flow that Claude cannot perform"
    env_vars:
      - name: GOOGLE_CALENDAR_TOKEN_JSON
        source: "Run `python scripts/get_calendar_token.py` in terminal — it opens a browser, signs in, and prints the JSON token to stdout"
    local_dev:
      - "python scripts/get_calendar_token.py"
      - "Copy the printed JSON and paste into .env as GOOGLE_CALENDAR_TOKEN_JSON=<paste>"

must_haves:
  truths:
    - "GOOGLE_CALENDAR_TOKEN_JSON is set in .env and is valid JSON"
    - "backend.actions.get_calendar_service() initializes without error when loaded"
    - "A Google Calendar event can be created programmatically"
  artifacts:
    - path: ".env"
      provides: "GOOGLE_CALENDAR_TOKEN_JSON env var"
      contains: "GOOGLE_CALENDAR_TOKEN_JSON"
  key_links:
    - from: ".env"
      to: "backend/main.py"
      via: "load_dotenv() + os.getenv('GOOGLE_CALENDAR_TOKEN_JSON')"
      pattern: "GOOGLE_CALENDAR_TOKEN_JSON"
---

<objective>
Get Google Calendar OAuth2 credentials into .env so calendar events fire in the demo.

Purpose: Calendar events are the most impressive autonomous action in the demo. Without OAuth2 credentials, meeting requests produce nothing.
Output: `.env` contains valid `GOOGLE_CALENDAR_TOKEN_JSON`; backend initializes calendar service without error.
</objective>

<execution_context>
@/Users/stevenyang/Documents/google-devpost-hackathon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stevenyang/Documents/google-devpost-hackathon/.claude/get-shit-done/templates/summary.md
@/Users/stevenyang/Documents/google-devpost-hackathon/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@scripts/get_calendar_token.py
@backend/actions.py
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify get_calendar_token.py is ready to run</name>
  <files>scripts/get_calendar_token.py</files>
  <read_first>scripts/get_calendar_token.py, credentials.json, .env.example</read_first>
  <action>
    Read scripts/get_calendar_token.py to confirm it handles the OAuth2 flow correctly.
    Check that credentials.json exists (required by the script for OAuth2 client config).
    Check .env.example to confirm GOOGLE_CALENDAR_TOKEN_JSON is listed as a required var.
    If credentials.json is missing, report this as a blocker — the script cannot run without it.
    Do NOT run the script — it requires browser interaction (checkpoint below).
  </action>
  <verify>
    - credentials.json exists at project root
    - scripts/get_calendar_token.py imports google_auth_oauthlib and reads credentials.json
  </verify>
  <acceptance_criteria>
    - credentials.json exists at project root
    - scripts/get_calendar_token.py references credentials.json
  </acceptance_criteria>
  <done>Script confirmed ready; credentials.json present</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-to-do>
    Run the OAuth2 script in your terminal:

    ```bash
    python scripts/get_calendar_token.py
    ```

    A browser will open. Sign in with your Google account and grant Calendar access.
    The script will print a JSON token to stdout. Copy the ENTIRE output (it starts with `{`).

    Then open .env and add:
    ```
    GOOGLE_CALENDAR_TOKEN_JSON={"token": "...", "refresh_token": "...", ...}
    ```

    Paste the full JSON as the value (no quotes around it unless it contains newlines — check .env.example for format).
  </what-to-do>
  <resume-signal>Type "done" when the token is in .env</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Validate token and calendar service init</name>
  <files>.env</files>
  <read_first>.env, backend/actions.py, backend/main.py</read_first>
  <action>
    1. Read .env and verify GOOGLE_CALENDAR_TOKEN_JSON is present and is valid JSON (parse it).
    2. Run a quick smoke test:
       ```bash
       python -c "
       import json, os
       from dotenv import load_dotenv
       load_dotenv()
       token = os.getenv('GOOGLE_CALENDAR_TOKEN_JSON')
       assert token, 'GOOGLE_CALENDAR_TOKEN_JSON is not set'
       parsed = json.loads(token)
       assert 'token' in parsed or 'access_token' in parsed, f'Unexpected token shape: {list(parsed.keys())}'
       from backend.actions import get_calendar_service
       svc = get_calendar_service(parsed)
       print('Calendar service initialized OK:', svc)
       "
       ```
    3. If the import fails or service init raises, report the error and stop.
  </action>
  <verify>python -c "import json,os; from dotenv import load_dotenv; load_dotenv(); t=os.getenv('GOOGLE_CALENDAR_TOKEN_JSON'); assert t; json.loads(t); print('Token valid JSON ✓')"</verify>
  <acceptance_criteria>
    - .env contains GOOGLE_CALENDAR_TOKEN_JSON
    - json.loads(token) succeeds without error
    - get_calendar_service(parsed) initializes without raising
  </acceptance_criteria>
  <done>Token is valid JSON and calendar service initializes successfully</done>
</task>

</tasks>

<verification>
- [ ] .env contains GOOGLE_CALENDAR_TOKEN_JSON
- [ ] `python -c "import json,os; from dotenv import load_dotenv; load_dotenv(); json.loads(os.getenv('GOOGLE_CALENDAR_TOKEN_JSON'))"` exits 0
- [ ] `python -c "from dotenv import load_dotenv; load_dotenv(); import json,os; from backend.actions import get_calendar_service; get_calendar_service(json.loads(os.getenv('GOOGLE_CALENDAR_TOKEN_JSON'))); print('OK')"` prints OK
</verification>

<success_criteria>
- GOOGLE_CALENDAR_TOKEN_JSON in .env is valid JSON
- Google Calendar service initializes without error
- Backend can create calendar events (validated in Phase 1 end-to-end after deploy)
</success_criteria>

<output>
After completion, create `.planning/phases/01-deploy-auth/01-01-SUMMARY.md`
</output>
