---
phase: 01-deploy-auth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [DEPLOY-01]
user_setup: []

must_haves:
  truths:
    - "Cloud Run URL is publicly accessible"
    - "GET <URL>/health returns {\"status\": \"ok\"}"
    - "WebSocket endpoint /ws/audio is reachable from browser"
  artifacts:
    - path: ".planning/phases/01-deploy-auth/cloud-run-url.txt"
      provides: "Public Cloud Run URL for submission proof"
      contains: "run.app"
  key_links:
    - from: "Dockerfile"
      to: "Cloud Run"
      via: "gcloud run deploy --source ."
      pattern: "PORT.*8080"
---

<objective>
Deploy the meeting agent to Google Cloud Run and capture the public URL for hackathon submission proof.

Purpose: Judges require a live deploy URL. Without it, submission is incomplete.
Output: Public Cloud Run URL accessible at `https://meeting-agent-*.run.app`; `/health` returns 200.
</objective>

<execution_context>
@/Users/stevenyang/Documents/google-devpost-hackathon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stevenyang/Documents/google-devpost-hackathon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@Dockerfile
@.env.example
@backend/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify gcloud auth and project config</name>
  <files></files>
  <read_first>Dockerfile, .env.example</read_first>
  <action>
    Run the following checks in order:
    1. `gcloud auth list` — verify an account is authenticated. If not, instruct user to run `gcloud auth login`.
    2. `gcloud config get-value project` — verify a project is set. If empty, instruct user to run `gcloud config set project <PROJECT_ID>`.
    3. `gcloud services list --enabled --filter="name:run.googleapis.com"` — verify Cloud Run API is enabled. If not enabled, run `gcloud services enable run.googleapis.com`.
    4. `gcloud services list --enabled --filter="name:speech.googleapis.com"` — verify Cloud Speech API is enabled. If not, run `gcloud services enable speech.googleapis.com`.
    5. `gcloud services list --enabled --filter="name:vision.googleapis.com"` — verify Cloud Vision API is enabled. If not, run `gcloud services enable vision.googleapis.com`.
    Report findings. If gcloud is not installed or not authed, stop and report blocker.
  </action>
  <verify>gcloud auth list 2>&1 | grep ACTIVE</verify>
  <acceptance_criteria>
    - gcloud auth list shows an ACTIVE account
    - gcloud config get-value project returns a non-empty project ID
    - Cloud Run API is enabled
  </acceptance_criteria>
  <done>gcloud authenticated, project set, required APIs enabled</done>
</task>

<task type="auto">
  <name>Task 2: Read env vars and deploy to Cloud Run</name>
  <files>.planning/phases/01-deploy-auth/cloud-run-url.txt</files>
  <read_first>.env, .env.example, Dockerfile</read_first>
  <action>
    1. Read .env and extract all required env vars: GOOGLE_API_KEY, GOOGLE_CLOUD_PROJECT, SLACK_BOT_TOKEN, SLACK_CHANNEL, GOOGLE_CALENDAR_TOKEN_JSON.
    2. Check each is present. If any is missing, list which ones and stop.
    3. Run the deploy command (replace each ... with the actual value read from .env):
       ```bash
       gcloud run deploy meeting-agent \
         --source . \
         --region us-central1 \
         --allow-unauthenticated \
         --memory 512Mi \
         --set-env-vars "GOOGLE_API_KEY=<value>,SLACK_BOT_TOKEN=<value>,SLACK_CHANNEL=<value>,GOOGLE_CLOUD_PROJECT=<value>,GOOGLE_CALENDAR_TOKEN_JSON=<value>"
       ```
       NOTE: GOOGLE_CALENDAR_TOKEN_JSON may contain commas inside the JSON — use --env-vars-file instead if so:
       Write all env vars to a temp file `/tmp/gsd-env-vars.yaml` in KEY: VALUE format, then:
       ```bash
       gcloud run deploy meeting-agent \
         --source . \
         --region us-central1 \
         --allow-unauthenticated \
         --memory 512Mi \
         --env-vars-file /tmp/gsd-env-vars.yaml
       ```
    4. Wait for deploy to complete. Extract the Service URL from output (line containing "Service URL:").
    5. Save URL to `.planning/phases/01-deploy-auth/cloud-run-url.txt`.
  </action>
  <verify>cat .planning/phases/01-deploy-auth/cloud-run-url.txt</verify>
  <acceptance_criteria>
    - cloud-run-url.txt contains a URL matching https://*.run.app
    - gcloud run deploy exited 0
  </acceptance_criteria>
  <done>Deploy succeeded; URL saved to cloud-run-url.txt</done>
</task>

<task type="auto">
  <name>Task 3: Verify live deployment</name>
  <files></files>
  <read_first>.planning/phases/01-deploy-auth/cloud-run-url.txt</read_first>
  <action>
    1. Read the URL from cloud-run-url.txt.
    2. Run `curl -s <URL>/health` and verify it returns `{"status": "ok"}`.
    3. Run `curl -s -o /dev/null -w "%{http_code}" <URL>/` and verify it returns 200.
    4. Print the URL prominently for the user to screenshot:
       ```
       ✅ DEPLOYMENT LIVE
       URL: <URL>
       Health: {"status": "ok"}
       Screenshot this for your hackathon submission!
       ```
  </action>
  <verify>curl -s $(cat .planning/phases/01-deploy-auth/cloud-run-url.txt)/health | grep '"status"'</verify>
  <acceptance_criteria>
    - curl /health returns {"status": "ok"}
    - curl / returns HTTP 200
    - URL is printed for user to screenshot
  </acceptance_criteria>
  <done>Live URL verified; {"status": "ok"} confirmed; URL printed for screenshot</done>
</task>

</tasks>

<verification>
- [ ] `.planning/phases/01-deploy-auth/cloud-run-url.txt` contains a https://*.run.app URL
- [ ] `curl -s $(cat .planning/phases/01-deploy-auth/cloud-run-url.txt)/health` returns `{"status":"ok"}`
- [ ] `curl -s -o /dev/null -w "%{http_code}" $(cat .planning/phases/01-deploy-auth/cloud-run-url.txt)/` returns 200
</verification>

<success_criteria>
- Cloud Run deployment succeeded
- Public URL is live and returns healthy response
- URL saved to cloud-run-url.txt for submission proof
</success_criteria>

<output>
After completion, create `.planning/phases/01-deploy-auth/01-02-SUMMARY.md`
</output>
