---
phase: 01.5-live-architecture-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/contracts.py
  - backend/main.py
  - backend/vision.py
  - static/index.html
autonomous: true
requirements:
  - VIZ-01
  - VIZ-06
must_haves:
  truths:
    - "Backend emits pipeline events (stt_start, stt_result, understanding_start, understanding_result, action_dispatched, vision_result) over existing /ws/audio WebSocket"
    - "Split-screen HTML layout exists with Canvas element (left 65%) and action cards panel (right 35%/w-80)"
    - "Pipeline WS message type is recognized by contracts and frontend dispatch"
  artifacts:
    - path: "backend/contracts.py"
      provides: "pipeline WsMessageType"
      contains: "pipeline"
    - path: "backend/main.py"
      provides: "Pipeline event emission at STT, understanding, and dispatch points"
      contains: "make_ws_message(\"pipeline\""
    - path: "backend/vision.py"
      provides: "Vision pipeline event forwarding to WebSocket"
      contains: "pipeline"
    - path: "static/index.html"
      provides: "Split-screen pipeline layout with Canvas element and pipeline actions panel"
      contains: "pipeline-canvas"
  key_links:
    - from: "backend/main.py"
      to: "backend/contracts.py"
      via: "make_ws_message with pipeline type"
      pattern: "make_ws_message.*pipeline"
    - from: "static/index.html"
      to: "static/app/pipeline.js"
      via: "script tag loading pipeline module"
      pattern: "pipeline\\.js"
---

<objective>
Add backend pipeline event emission and split-screen HTML layout for the live architecture visualization.

Purpose: Create the data pipeline (backend events over WS) and DOM structure (Canvas + actions panel) that the Canvas renderer (Plan 02) will consume.
Output: Backend emits pipeline events at each processing stage; index.html has split-screen layout with Canvas element and pipeline actions feed.
</objective>

<execution_context>
@/Users/stevenyang/Documents/google-devpost-hackathon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stevenyang/Documents/google-devpost-hackathon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.5-live-architecture-visualization/01.5-CONTEXT.md
@.planning/phases/01.5-live-architecture-visualization/01.5-RESEARCH.md
@.planning/phases/01.5-live-architecture-visualization/01.5-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From backend/contracts.py:
```python
WsMessageType = Literal["transcript", "interim", "status", "sentiment", "action", "done"]

def make_ws_message(message_type: WsMessageType, data: object | None = None) -> WsServerMessage:
    message: WsServerMessage = {"type": message_type}
    if data is not None:
        message["data"] = data
    return message
```

From backend/main.py:
```python
async def send(msg: dict) -> bool:
    """Send JSON to client. Returns False if client is gone."""

async def on_transcript(text: str) -> None:
    # Sends transcript, status, triggers understanding + dispatch

async def _handle_understanding(understanding: UnderstandingResult) -> None:
    # Logs understanding, sends sentiment, dispatches actions

async def _dispatch(understanding: UnderstandingResult) -> None:
    # Dispatches actions via session.dispatch, sends action messages
```

From backend/vision.py:
```python
async def analyze_frame(frame_bytes: bytes, session_state: VisionState | None = None) -> dict | None:
    # Returns vision result dict or None (debounced)
```

From static/index.html:
```html
<!-- Current meeting screen has: transcript (left flex-1) + actions (right w-80) -->
<!-- Bottom nav has: Home | Meeting | Actions -->
<!-- Scripts loaded: core.js, render.js, documents.js, media.js, session.js, app.js -->
```

From static/app/core.js:
```javascript
contracts: {
    wsTypes: new Set(['transcript', 'interim', 'status', 'sentiment', 'action', 'done']),
    actionTypes: new Set(['slack', 'calendar', 'task', 'document']),
},
```

From static/app/session.js:
```javascript
function handleWsMessage(message) {
    if (!message || !contracts.wsTypes.has(message.type)) return;
    // switch on message.type: done, transcript, interim, action, sentiment, status
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pipeline WsMessageType and emit pipeline events in backend</name>
  <files>backend/contracts.py, backend/main.py, backend/vision.py</files>
  <read_first>
    - backend/contracts.py (current WsMessageType, make_ws_message)
    - backend/main.py (on_transcript, _handle_understanding, _dispatch, api_frame)
    - backend/vision.py (analyze_frame function)
    - .planning/phases/01.5-live-architecture-visualization/01.5-RESEARCH.md (Pattern 5: Pipeline Event Protocol)
  </read_first>
  <action>
**contracts.py changes:**
1. Add `"pipeline"` to the `WsMessageType` Literal type:
   ```python
   WsMessageType = Literal["transcript", "interim", "status", "sentiment", "action", "done", "pipeline"]
   ```

**main.py changes — emit pipeline events at each processing stage:**

The `send` function is a closure inside `websocket_audio`. All pipeline event emissions go through the same `send()` function. Add pipeline event emissions at these exact points:

2. In `on_transcript`, right after `_transcript_segments.append(text)` and before the `send(make_ws_message("transcript"...))` call, add:
   ```python
   await send(make_ws_message("pipeline", {"event": "stt_result", "stats": {"words": len(text.split())}}))
   ```

3. In `on_interim`, before the existing send call, add:
   ```python
   await send(make_ws_message("pipeline", {"event": "stt_start"}))
   ```

4. In `_handle_understanding`, right before the `logger.info("[%s] Understanding result:..."` line, add:
   ```python
   await send(make_ws_message("pipeline", {"event": "understanding_result", "stats": {
       "sentiment": understanding.get("sentiment", "neutral"),
       "actions": sum(len(understanding.get(k, [])) for k in ("commitments", "agreements", "meeting_requests", "document_revisions"))
   }}))
   ```

5. In `_handle_understanding`, right before calling `buf.process(text, face, on_result=_handle_understanding)` (the line `understanding = await buf.process(...)`), add:
   ```python
   await send(make_ws_message("pipeline", {"event": "understanding_start"}))
   ```

6. In `_dispatch`, inside the `_on_action` callback, after the existing `await send(make_ws_message("action", action))` line, add:
   ```python
   await send(make_ws_message("pipeline", {"event": "action_dispatched", "action_type": action["type"]}))
   ```

**vision.py changes — forward vision results to WebSocket:**

7. In `api_frame` handler in `main.py` (NOT vision.py), after `result = await analyze_frame(...)` and before `return JSONResponse(result or {})`, add pipeline event emission. BUT `api_frame` is a REST handler with no access to the WS `send` function. Instead, store the vision result in session_state and have a helper emit it.

   Approach: In `main.py` `api_frame` handler, after getting the result, if result is not None and session_state exists, store a pipeline event in session_state that the WS loop can pick up. Simpler approach: emit the vision pipeline event directly in the `api_frame` handler by looking up the session's WebSocket send function.

   Actually, the cleanest approach per RESEARCH.md: the frontend already knows when it sends a frame and gets a response. BUT we want backend-driven events. Store a reference to `send` on the session_state so `api_frame` can emit pipeline events:

   In `websocket_audio`, after `session_state = session_registry.ensure(session_id)`, add:
   ```python
   session_state.ws_send = send
   ```

   In `api_frame`, after `result = await analyze_frame(...)`, add:
   ```python
   if result and session_state and hasattr(session_state, 'ws_send') and session_state.ws_send:
       await session_state.ws_send(make_ws_message("pipeline", {
           "event": "vision_result",
           "stats": {
               "sentiment": result.get("sentiment", "neutral"),
               "score": round(result.get("engagement", 0) or 0, 2)
           }
       }))
   ```

   Note: `session_state.ws_send` needs to be set to `None` in the finally block of `websocket_audio` to prevent stale references:
   ```python
   # In finally block, before session_registry.discard:
   if hasattr(session_state, 'ws_send'):
       session_state.ws_send = None
   ```
  </action>
  <verify>
    <automated>cd /Users/stevenyang/Documents/google-devpost-hackathon && python -c "from backend.contracts import WsMessageType; print('pipeline' in WsMessageType.__args__)" && python -c "import ast; tree = ast.parse(open('backend/main.py').read()); source = open('backend/main.py').read(); assert 'make_ws_message(\"pipeline\"' in source, 'No pipeline events in main.py'; print('OK: pipeline events found in main.py')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/contracts.py WsMessageType contains "pipeline" literal: `grep -q '"pipeline"' backend/contracts.py`
    - backend/main.py contains at least 5 pipeline event emissions: `grep -c 'make_ws_message("pipeline"' backend/main.py` returns >= 5
    - backend/main.py emits stt_start event: `grep -q '"event": "stt_start"' backend/main.py`
    - backend/main.py emits stt_result event: `grep -q '"event": "stt_result"' backend/main.py`
    - backend/main.py emits understanding_start event: `grep -q '"event": "understanding_start"' backend/main.py`
    - backend/main.py emits understanding_result event: `grep -q '"event": "understanding_result"' backend/main.py`
    - backend/main.py emits action_dispatched event: `grep -q '"event": "action_dispatched"' backend/main.py`
    - backend/main.py emits vision_result via session_state.ws_send: `grep -q 'vision_result' backend/main.py`
    - backend/main.py cleans up ws_send in finally block: `grep -q 'ws_send = None' backend/main.py`
    - Python imports succeed: `python -c "from backend.contracts import make_ws_message; print('OK')"`
  </acceptance_criteria>
  <done>Backend emits 6 pipeline event types (stt_start, stt_result, understanding_start, understanding_result, action_dispatched, vision_result) over existing /ws/audio WebSocket using the new "pipeline" WsMessageType. Vision events forwarded via session_state.ws_send.</done>
</task>

<task type="auto">
  <name>Task 2: Add split-screen pipeline layout to index.html and register pipeline WS type in frontend</name>
  <files>static/index.html, static/app/core.js, static/app/session.js</files>
  <read_first>
    - static/index.html (current screen-meeting section, bottom nav, script tags)
    - static/app/core.js (dom object, contracts.wsTypes, state object)
    - static/app/session.js (handleWsMessage function, startMeeting screen switch)
    - .planning/phases/01.5-live-architecture-visualization/01.5-UI-SPEC.md (Screen Layout Summary, Copywriting Contract)
  </read_first>
  <action>
**index.html changes:**

1. Add a new screen section `screen-pipeline` AFTER the `screen-meeting` section closing `</section>` and BEFORE the `screen-summary` section. Use the exact layout from UI-SPEC:

```html
<!-- ===============================================================
     SCREEN: PIPELINE VISUALIZATION
     =============================================================== -->
<section id="screen-pipeline" class="screen flex-col flex-1">

  <!-- Header (matches meeting screen header) -->
  <header class="flex items-center justify-between h-12 px-4 border-b border-border-muted bg-surface/50 backdrop-blur-md sticky top-0 z-10">
    <div class="flex items-center gap-3">
      <h1 class="text-sm font-bold tracking-tight text-white">Meeting Intelligence</h1>
      <div id="pipeline-status-chip" class="flex items-center gap-1.5 px-2 py-0.5 rounded border border-border-muted bg-bg-dark text-[10px] text-slate-400 font-mono uppercase tracking-wider">
        <span class="material-symbols-outlined text-[12px]">settings_input_antenna</span>
        <span id="pipeline-status-text">Connecting...</span>
      </div>
      <div id="pipeline-sentiment-pill" class="hidden items-center gap-1.5 px-2 py-0.5 rounded border border-success/50 bg-success/10 text-[10px] text-success font-mono uppercase tracking-wider">
        <span class="material-symbols-outlined text-[12px]">mood</span>
        <span id="pipeline-sentiment-text">NEUTRAL</span>
      </div>
    </div>
    <button id="pipeline-stop-btn" class="flex items-center gap-1.5 px-3 py-1 rounded-lg bg-danger/10 border border-danger/30 text-danger text-xs font-bold hover:bg-danger/20 transition-colors">
      <span class="material-symbols-outlined text-sm">stop_circle</span>
      Stop
    </button>
  </header>

  <!-- Mic level bar -->
  <div class="w-full bg-surface h-[6px] relative">
    <div id="pipeline-mic-level" class="absolute inset-0 bg-success/60 w-0 transition-all duration-75"></div>
  </div>

  <!-- Split content: Pipeline Canvas (left 65%) + Action Cards (right 35%) -->
  <div class="flex flex-1 overflow-hidden">
    <!-- Left: Pipeline Canvas -->
    <div class="flex-1 flex items-center justify-center bg-bg-dark p-4 min-h-0 relative">
      <canvas id="pipeline-canvas" class="w-full h-full"></canvas>
      <!-- Empty state overlay -->
      <div id="pipeline-empty-state" class="absolute inset-0 flex flex-col items-center justify-center pointer-events-none">
        <p class="text-sm text-slate-500 font-medium">Waiting for pipeline activity...</p>
        <p class="text-xs text-slate-600 mt-1">Start speaking to see data flow through the architecture in real-time.</p>
      </div>
    </div>
    <!-- Right: Action Cards -->
    <div class="w-80 flex flex-col bg-surface border-l border-border-muted min-h-0">
      <div class="px-4 py-2 border-b border-border-muted">
        <h2 class="text-[10px] font-semibold uppercase tracking-widest text-slate-500">Live Actions</h2>
      </div>
      <div id="pipeline-actions" class="flex-1 overflow-y-auto p-3 space-y-2"></div>
    </div>
  </div>
</section>
```

2. Update the bottom nav to add a Pipeline tab. Replace the existing `<nav id="bottom-nav">` contents. The three tabs become four: Home | Pipeline | Meeting | Actions:

```html
<nav id="bottom-nav" class="flex gap-2 border-t border-border-muted bg-surface px-4 pb-[max(0.5rem,env(safe-area-inset-bottom))] pt-2 flex-shrink-0">
  <a data-nav="home" class="flex flex-1 flex-col items-center justify-end gap-1 text-primary cursor-pointer">
    <span class="material-symbols-outlined text-xl">home</span>
    <p class="text-[10px] font-bold">Home</p>
  </a>
  <a data-nav="pipeline" class="flex flex-1 flex-col items-center justify-end gap-1 text-slate-400 hover:text-white transition-colors cursor-pointer">
    <span class="material-symbols-outlined text-xl">hub</span>
    <p class="text-[10px] font-medium">Pipeline</p>
  </a>
  <a data-nav="meeting" class="flex flex-1 flex-col items-center justify-end gap-1 text-slate-400 hover:text-white transition-colors cursor-pointer">
    <span class="material-symbols-outlined text-xl">videocam</span>
    <p class="text-[10px] font-medium">Meeting</p>
  </a>
  <a data-nav="summary" class="flex flex-1 flex-col items-center justify-end gap-1 text-slate-400 hover:text-white transition-colors cursor-pointer">
    <span class="material-symbols-outlined text-xl">checklist</span>
    <p class="text-[10px] font-medium">Actions</p>
  </a>
</nav>
```

3. Add the pipeline.js script tag AFTER session.js and BEFORE app.js:
```html
<script src="/static/app/pipeline.js?v=7"></script>
```

4. Add custom scrollbar styles for `#pipeline-actions` by adding `#pipeline-actions::-webkit-scrollbar` rules alongside the existing scrollbar rules in the `<style>` block. Copy the same pattern used for `#actions-feed`.

**core.js changes:**

5. Add `pipeline` to the `dom.screens` object:
   ```javascript
   pipeline: document.getElementById('screen-pipeline'),
   ```

6. Add pipeline-specific DOM refs to the `dom` object:
   ```javascript
   pipelineCanvas: document.getElementById('pipeline-canvas'),
   pipelineActions: document.getElementById('pipeline-actions'),
   pipelineStatusText: document.getElementById('pipeline-status-text'),
   pipelineSentimentPill: document.getElementById('pipeline-sentiment-pill'),
   pipelineSentimentText: document.getElementById('pipeline-sentiment-text'),
   pipelineMicLevel: document.getElementById('pipeline-mic-level'),
   pipelineStopBtn: document.getElementById('pipeline-stop-btn'),
   pipelineEmptyState: document.getElementById('pipeline-empty-state'),
   ```

7. Add `'pipeline'` to `contracts.wsTypes`:
   ```javascript
   wsTypes: new Set(['transcript', 'interim', 'status', 'sentiment', 'action', 'done', 'pipeline']),
   ```

**session.js changes:**

8. In `handleWsMessage`, add a case for the `pipeline` message type. Add this BEFORE the existing `if (message.type === 'done')` check:
   ```javascript
   if (message.type === 'pipeline') {
     if (window.MeetingAgent.pipeline) {
       window.MeetingAgent.pipeline.handlePipelineEvent(message.data);
     }
     return;
   }
   ```

9. In `startMeeting`, change `render.showScreen('meeting')` to `render.showScreen('pipeline')` so meetings open to the pipeline view by default (per CONTEXT.md: "Pipeline auto-shows when meeting starts").

10. Wire the pipeline stop button: In `init()`, after the `dom.stopBtn.addEventListener(...)` line, add:
    ```javascript
    if (dom.pipelineStopBtn) {
      dom.pipelineStopBtn.addEventListener('click', () => stopMeeting('user'));
    }
    ```

11. In `handleWsMessage`, for action messages, also render to pipeline-actions panel. Update the `if (message.type === 'action')` block:
    ```javascript
    if (message.type === 'action') {
      render.handleActionMessage(message.data);
      if (dom.pipelineActions) {
        render.createActionCard(message.data, dom.pipelineActions);
      }
    }
    ```
    Wait -- `createActionCard` is on `render` but it uses the `dom.actionsFeed` container. The function already accepts a `container` parameter. So this should work. BUT we need to export `createActionCard` from render.js. Check render.js -- it's already defined there but not exported on `window.MeetingAgent.render`. Add it to the exports.

12. In render.js, add `createActionCard` to the exports object at the bottom:
    ```javascript
    createActionCard,
    ```

13. In render.js `handleStatusMessage`, also update the pipeline status text:
    ```javascript
    function handleStatusMessage(data) {
      const text = (data && data.text) || String(data);
      dom.statusText.textContent = text;
      if (dom.pipelineStatusText) dom.pipelineStatusText.textContent = text;
    }
    ```

14. In render.js `updateSentiment`, also update the pipeline sentiment pill. After updating the main sentiment pill, add:
    ```javascript
    if (dom.pipelineSentimentPill) {
      dom.pipelineSentimentPill.classList.remove('hidden');
      dom.pipelineSentimentPill.className = dom.sentimentPill.className;
      dom.pipelineSentimentPill.querySelector('.material-symbols-outlined').textContent = config.icon;
    }
    if (dom.pipelineSentimentText) {
      dom.pipelineSentimentText.textContent = sentiment.toUpperCase();
    }
    ```
  </action>
  <verify>
    <automated>cd /Users/stevenyang/Documents/google-devpost-hackathon && grep -q 'screen-pipeline' static/index.html && grep -q 'pipeline-canvas' static/index.html && grep -q 'pipeline-actions' static/index.html && grep -q "data-nav=\"pipeline\"" static/index.html && grep -q "'pipeline'" static/app/core.js && grep -q "pipeline" static/app/session.js && grep -q 'pipeline.js' static/index.html && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains screen-pipeline section: `grep -q 'id="screen-pipeline"' static/index.html`
    - index.html contains pipeline-canvas element: `grep -q 'id="pipeline-canvas"' static/index.html`
    - index.html contains pipeline-actions container: `grep -q 'id="pipeline-actions"' static/index.html`
    - index.html contains Pipeline nav tab with hub icon: `grep -q 'data-nav="pipeline"' static/index.html`
    - index.html loads pipeline.js script: `grep -q 'pipeline.js' static/index.html`
    - index.html has LIVE ACTIONS header: `grep -q 'Live Actions' static/index.html`
    - index.html has empty state text: `grep -q 'Waiting for pipeline activity' static/index.html`
    - core.js registers pipeline screen in dom.screens: `grep -q 'pipeline:' static/app/core.js`
    - core.js adds pipeline to wsTypes: `grep -q "'pipeline'" static/app/core.js`
    - session.js handles pipeline message type: `grep -q "message.type === 'pipeline'" static/app/session.js`
    - session.js shows pipeline screen on meeting start: `grep -q "showScreen('pipeline')" static/app/session.js`
    - render.js exports createActionCard: `grep -q 'createActionCard' static/app/render.js`
  </acceptance_criteria>
  <done>Split-screen pipeline layout exists in index.html with Canvas element and actions panel. Frontend recognizes "pipeline" WS message type and routes to pipeline module. Meeting start shows pipeline screen. Nav has Pipeline tab.</done>
</task>

</tasks>

<verification>
1. `python -c "from backend.contracts import make_ws_message; msg = make_ws_message('pipeline', {'event': 'stt_start'}); assert msg['type'] == 'pipeline'; print('Backend contracts OK')"` passes
2. `grep -c 'make_ws_message("pipeline"' backend/main.py` returns >= 5
3. `grep -q 'id="pipeline-canvas"' static/index.html` succeeds
4. `grep -q "'pipeline'" static/app/core.js` succeeds
5. No existing functionality broken: existing screen-meeting and screen-summary sections unchanged in structure
</verification>

<success_criteria>
- Backend emits pipeline events at 6 processing stages (stt_start, stt_result, understanding_start, understanding_result, action_dispatched, vision_result)
- index.html has split-screen pipeline section with Canvas + actions panel
- Frontend dispatch recognizes pipeline message type
- Pipeline screen auto-shows when meeting starts
- Bottom nav includes Pipeline tab with hub icon
</success_criteria>

<output>
After completion, create `.planning/phases/01.5-live-architecture-visualization/01.5-01-SUMMARY.md`
</output>
